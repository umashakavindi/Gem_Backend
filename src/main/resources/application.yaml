spring:
  datasource:
    url: jdbc:h2:mem:gemdb
    usernam: admin
    password:
  h2:
    console:
      enabled: true
    path: /h2-console
  jpa:
    hibernate:
      dbl-auto: create-drop
    show-sql: true

server:
  port: 8080

